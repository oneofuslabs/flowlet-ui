{
  "rules": [
    {
      "name": "package-manager",
      "description": "Always use pnpm as the package manager instead of npm or yarn",
      "globs": "**/*",
      "content": "Always use pnpm for all package management operations. Never use npm or yarn commands. For example, use 'pnpm add' instead of 'npm install', 'pnpm run dev' instead of 'npm run dev', etc."
    }
  ]
} 